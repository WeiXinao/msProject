syntax = "proto3";

package department;
option go_package="./department";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Department {
  rpc Ping(Request) returns(Response);
}
